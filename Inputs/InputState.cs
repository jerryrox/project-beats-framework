namespace PBFramework.Inputs
{
    /// <summary>
    /// Defines the types of states any input methods may have.
    /// </summary>
    public enum InputState
    {
        Idle = 0,
        Press,
        Hold,
        Release,
    }
}